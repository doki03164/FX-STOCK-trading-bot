# ---------------------------------------------------------------- generated data
# Reproducible with fetch/sweep and ~1 GB in total, so it stays out of the repo.
# start.py rebuilds whatever is missing on first launch.
data/
sweep*.csv
candidates*.parquet
dashboard*.html
dashboard_data*.json
plan*.json
events*.jsonl
state*.json
*_log.txt
orders.jsonl        # your own trade log — personal, never committed

# ---------------------------------------------------------------- python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# ---------------------------------------------------------------- editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
